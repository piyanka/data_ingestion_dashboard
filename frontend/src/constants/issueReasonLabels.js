export const issueReasonLabels = {
  quantity_is_zero: "Expected quantity > 0, but the normalized value is 0.",
  quantity_below_zero: "Expected quantity > 0, but the normalized value is less than 0.",
};